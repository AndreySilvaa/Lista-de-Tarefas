import React from 'react'

// CSS
import styles from './Footer.module.css'

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>React + TS ToDo <span>@2023</span></p>

    </footer>
  )
}

export default Footer